import React from "react";
import ReactDOM from "react-dom/client";
import SnatcherDashboard from "./SnatcherDashboard";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SnatcherDashboard />
  </React.StrictMode>
);
